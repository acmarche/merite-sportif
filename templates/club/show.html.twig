{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
    <div class="card bg-light mb-3">
        <div class="card-header">
            <h2>{{ club }}</h2>
        </div>
        <div class="card-body">

            {% include 'club/_actions.html.twig' %}



        </div>
    </div>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
