<div class="d-print-none">
    <a href="{{ path('candidat_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
        Retour à la liste
    </a>

    <a href="{{ path('candidat_edit', { 'id': candidat.id }) }}" role="button"
       class="btn btn-primary"><i class="far fa-edit"></i> Editer</a>

    {% if is_granted('ROLE_MERITE_ADMIN') %}
        <button class="btn btn-danger" role='button' data-toggle="modal" data-target="#deleteModal"><i
                    class="fas fa-trash"></i>
            Supprimer
        </button>
    {% endif %}

    <br/>
    <br/>
</div>