{% if is_granted('ROLE_MERITE_CLUB') %}
    <li>
        <a class="text-white" href="{{ path('proposition_index') }}">Proposer des candidats</a>
    </li>
    {% if vote_activate == true %}
        <li>
            <a class="text-white" href="{{ path('vote_show') }}">Mon vote</a>
        </li>
    {% endif %}
{% endif %}

{% if is_granted('ROLE_MERITE_ADMIN') %}
    <li class="nav-item dropdown">
        <a class="nav-link2 dropdown-toggle text-white" href="#" id="dropdown01" data-toggle="dropdown"
           aria-haspopup="true"
           aria-expanded="false" title="Paramètres"> Paramètres</a>
        <div class="dropdown-menu" aria-labelledby="dropdown01">
            <a class="dropdown-item tex2t-white"
               href="{{ path('candidat_index') }}">Les candidats</a>
            <a class="dropdown-item tex2t-white"
               href="{{ path('club_index') }}">Les clubs</a>
            <a class="dropdown-item text-2white"
               href="{{ path('sport_index') }}">Liste des sports</a>
            <a class="dropdown-item text-w2hite"
               href="{{ path('categorie_index') }}">Les catégories</a>
            <a class="dropdown-item text-whit2e"
               href="{{ path('merite_user_index') }}">Les utilisateurs</a>
            <a class="dropdown-item text-wh2ite"
               href="{{ path('merite_message_index') }}">Envoie des mails</a>
            <a class="dropdown-item text-w2hite" href="{{ path('merite_token_create') }}">
                Générer les tokens</a>
        </div>
    </li>
{% endif %}